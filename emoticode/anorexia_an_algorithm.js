/*
 Anorexia, an algorithm
*/ 

let any1 extends Person 
let me extends Person:not(any1)
let duration.days = 1
let duration.months = 3
let duration.years = 26
let myTarget = NULL

if (this.getLimit) {
return caloricIntake
}


any1.getLimit() = 2000
me.setLimit( any1 — betterSafeThanSorry )
me.getLimit() = 1200 


for (day in time.days){
let target = randBetween( 0 , me.currentWeight )
me.setLimit( me.getLimit() — any1.getLimit() / 2 )
me.getLimit().decreaseBy(day)
}

if (myTarget == me.currentWeight ){
return new Target(myTarget - 10)
} else {
return "ERROR 400: Bad Request: variable type 'Person' cannot be divided by zero"

}